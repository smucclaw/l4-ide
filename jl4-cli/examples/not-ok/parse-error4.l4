§ `The factorial function`

-- Report single error, even though we could see there are two issues
-- Namely MEANS and a missing 'ELSE' branch.
-- Error recovery only happens over 'topdecl'
GIVEN n IS A NUMBER
GIVETH A NUMBER
DECIDE factorial MEANS n
  IS
    IF n EQUALS 0
    THEN 1
