# This directory contains WASM build artifacts.
# Files are generated by: npm run maybe-build-wasm
# Or manually: cd jl4-wasm && ./scripts/build-wasm.sh --all
#
# The WASM files are not committed to git - they are built on demand
# when VITE_PREFER_WASM=true is set in the environment.
