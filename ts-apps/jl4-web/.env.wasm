# WASM Mode Configuration
# Use this file for local development with WASM: cp .env.wasm .env.local

# Enable WASM mode (in-browser LSP, no server required)
VITE_PREFER_WASM=true

# WASM binary and JS FFI glue code
VITE_WASM_URL=/wasm/jl4-core.wasm
VITE_WASM_JS_URL=/wasm/jl4-core.mjs
VITE_WASM_VERSION=dev

# Note: WebSocket URL is still needed as fallback
VITE_SOCKET_URL=ws://localhost:5007

# Session Persistence Server
VITE_SESSION_URL=http://localhost:5008

# Decision Service URL  
VITE_DECISION_SERVICE_URL=http://localhost:8001

# Wizard Application URL
VITE_WIZARD_URL=http://localhost:5174
