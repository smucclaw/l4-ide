`meeting time`
`deadline`
`london meeting`
`start of day`
`Date of` with `meeting time`
`Time of` with `meeting time`
`Timezone of` with `meeting time`
`the hour of` with `meeting time`
`the minute of` with `meeting time`
`the day of` with `meeting time`
`the month of` with `meeting time`
`the year of` with `meeting time`
`Datetime to serial` with `meeting time`
`is afternoon` with `meeting time`
`is morning` with `start of day`
`at midnight` with `meeting time`
`at noon` with `meeting time`
`at` with `meeting time` and `TIME_FROM_HMS` with 15, 0 and 0
`the earlier of` with `meeting time` and `deadline`
`the later of` with `meeting time` and `deadline`
`meeting before deadline`
`deadline after meeting`
`leq datetime`
`geq datetime`
`is before datetime`
`is after datetime`
`sg vs london`
`TIMEZONE`
`plus 30 days`
`minus 10 days`
`next day`
`prev day`
`next month`
`next year`
`date less`
`date greater`
`date leq`
`date equal`
`days diff`
`same date check`
`the earlier of` with `date1` and `date2`
`the later of` with `date1` and `date2`
