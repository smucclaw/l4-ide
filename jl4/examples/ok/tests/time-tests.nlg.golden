`morning time`
`afternoon time`
`midnight`
`early morning`
`evening time`
`midnight via am`
`noon via pm`
`the hour of` with `morning time`
`the minute of` with `morning time`
`the second of` with `morning time`
`_ Time to serial` with `Time` with 12, 0 and 0
`_ Serial to time` with 0.5
`_ is morning` with `morning time`
`_ is afternoon` with `afternoon time`
`_ is evening` with `Time` with 20, 0 and 0
`_ is before noon` with `morning time`
`_ is after noon` with `afternoon time`
`_ plus hours _` with `morning time` and 2
`_ plus minutes _` with `morning time` and 30
`_ minus hours _` with `morning time` and 1
`morning before afternoon`
`afternoon after morning`
`the earlier of` with `morning time` and `afternoon time`
`the later of` with `morning time` and `afternoon time`
`leq check`
`geq check`
`lt check`
`gt check`
`is before check`
`is after check`
the difference between `morning time` and `afternoon time`
