consider the case distinctions of `choice` :  when `Left` has `a` then This is attached to `a` with This is also attached to `a`. when `Right` has `b` then `Right` with Might be attached to %right b% in the future
