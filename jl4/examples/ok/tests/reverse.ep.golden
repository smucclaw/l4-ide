GIVEN
  a    IS A TYPE
  list IS A LIST OF a
GIVETH A LIST OF a
reverse list MEANS go EMPTY list
  WHERE
    go acc l MEANS
      CONSIDER l
      WHEN EMPTY            THEN acc
      WHEN x FOLLOWED BY xs THEN go (x FOLLOWED BY acc) xs
