-- Computed Fields: Type Mismatch (SHOULD FAIL)
-- MEANS expression type doesn't match declared field type.

IMPORT prelude

DECLARE Bad HAS
    `x` IS A NUMBER
    `y` IS A BOOLEAN
        MEANS `x` + 1
